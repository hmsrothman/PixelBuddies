#version 400 core

layout (location = 0) in vec2 pos;
layout (location = 0) in vec2 pos;
layout (location = 0) in vec2 pos;


out vec4 fragColor;
out vec2 fragUV;

void main(){
	gl_Position.xy = pos.xy;
	gl_Position.z = 0;
	gl_Position.w = 1;
	fragColor = vec4(1);
	fragUV = vec2(0);
}